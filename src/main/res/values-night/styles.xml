<resources>
    <!-- 定义dialog的样式-->
    <style name="CustomDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@drawable/dialog_background_night</item>
    </style>
</resources>